import { variable } from "./context";
export declare function getAllIndexes(arr: string[], val: string): Array<number>;
export declare function curl(start: number[], end: number[], distribute: string[]): string[];
export declare const variableIncludes: (id: string, variable: variable[]) => boolean;
